{
  "name": "instatory",
  "version": "1.0.0",
  "description": "Instatory is a web application that integrates a frontend React application with a backend Flask server to process images and manage an inventory database. Users can upload images, initiate processing, and view the results.",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend-app && npm start",
    "dev:backend": "cd backend && flask run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
